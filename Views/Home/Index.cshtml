@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    @foreach(var item in @ViewBag.Cards) {
        <a href='@Url.Action("Pelicula", "Home",new {pelicula=@item.IdCard})'>@item.Titulo</a>
    }
</div>
